SECRET_KEY=wger-docker-supersecret-key-1234567890!@#$%^&*(-_)
SITE_URL=http://localhost
TIME_ZONE=Europe/Berlin
TZ=Europe/Berlin

# Postgres — used by both the db service and Django's DB connection
POSTGRES_USER=wger
POSTGRES_PASSWORD=wger
POSTGRES_DB=wger
PS_DATABASE_URI=postgres://wger:wger@db:5432/wger

# Django DB + cache
DJANGO_PERFORM_MIGRATIONS=True
DJANGO_CACHE_BACKEND=django_redis.cache.RedisCache
DJANGO_CACHE_LOCATION=redis://cache:6379/1
DJANGO_CACHE_TIMEOUT=1296000
DJANGO_CACHE_CLIENT_CLASS=django_redis.client.DefaultClient
DJANGO_DEBUG=False

# App settings
WGER_USE_GUNICORN=True
WGER_PORT=8000
WGER_INSTANCE=https://wger.de
ALLOW_REGISTRATION=True
ALLOW_GUEST_USERS=False
ALLOW_UPLOAD_VIDEOS=True
MIN_ACCOUNT_AGE_TO_TRUST=21
DOWNLOAD_INGREDIENTS_FROM=WGER

# Disable Celery — no workers in testbot setup
USE_CELERY=False

# Disable brute-force lockout — testbot makes repeated auth calls
AXES_ENABLED=False

# Token lifetimes
ACCESS_TOKEN_LIFETIME=10
REFRESH_TOKEN_LIFETIME=2880
